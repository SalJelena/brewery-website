import styled from "styled-components";

const StyledPages = styled.button`
    cursor: pointer;
    padding: 5px;
    font-size: 20px;
    margin: 5px;
    border: none;
    background-color: none;

    &:hover{
        transform: scale(1.2)
    }

`

export default StyledPages